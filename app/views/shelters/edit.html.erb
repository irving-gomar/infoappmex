<%= simple_form_for [@ong, @shelter] do |f| %>
  <%= f.input :name %>
  <%= f.input :description, as: :text %>
  <%= f.input :address %>
  <%= f.input :max_capacity %>
  <%= f.button :submit %>
<% end %>

<%= link_to shelter_path(@shelter), class: 'btn btn-info' do %>
    Volver a ONG
<% end %>